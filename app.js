import { VERSION, TOPICS, KINDS } from './data.js';
import { STORAGE_KEY,escapeHTML as h,safeURL,emptyStore,normalizeStore,allRooms,allVoices,roomVoices,roomStats,groups,getClaimState,filterRooms,makeVoice,makeRoom,removeVoice,toggleFollow } from './core.js';
const $=s=>document.querySelector(s),main=$('#main'),modal=$('#modal');
let store=emptyStore(),storageWarning=false;
try{const raw=localStorage.getItem(STORAGE_KEY);if(raw)store=normalizeStore(JSON.parse(raw))}catch{storageWarning=true}
let topic='ทั้งหมด',feedTab='discover',query='',roomTab='orbit',kindFilter='',lastRoom='',toastTimer,lastFocus=null;
const icons={plus:'<path d="M12 5v14M5 12h14"/>',follow:'<circle cx="12" cy="12" r="8"/><path d="M12 8v8M8 12h8"/>',share:'<path d="M9 5H5v14h14v-4M12 4h8v8M10 14 20 4"/>',arrow:'<path d="M4 12h16m-6-6 6 6-6 6"/>'};
const icon=name=>`<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${icons[name]||icons.arrow}</svg>`;
const avatar=(name,i=0)=>`<span class="avatar ${['','avatar-purple','avatar-amber','avatar-lime'][i%4]}" aria-hidden="true">${h(Array.from(name)[0]||'ค')}</span>`;
const cls=s=>({SUPPORTED:'supported',DISPUTED:'disputed',UNKNOWN:'unknown',COUNTERED:'disputed'}[s]||'unknown');
const stateLabel=s=>({SUPPORTED:'มีข้อมูลสนับสนุน',DISPUTED:'มีข้อมูลขัดแย้ง',UNKNOWN:'ยังไม่ทราบ',COUNTERED:'มีข้อมูลคัดค้าน'}[s]||'ยังไม่ทราบ');
const stateMark=s=>({SUPPORTED:'◈',DISPUTED:'◐',UNKNOWN:'?',COUNTERED:'◐'}[s]||'?');
function save(next){try{localStorage.setItem(STORAGE_KEY,JSON.stringify(next));store=next;return true}catch{toast('บันทึกไม่ได้: พื้นที่ในเครื่องเต็มหรือเบราว์เซอร์ปิดการจัดเก็บ');return false}}
function toast(text){const el=$('#toast');el.textContent=text;el.classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>el.classList.remove('show'),4200)}
function openModal(content){if(!modal.open)lastFocus=document.activeElement;$('#modal-content').innerHTML=`<div class="modal-inner">${content}</div>`;if(!modal.open)modal.showModal()}
function modalHead(title){return `<div class="modal-head"><h2>${h(title)}</h2><button class="iconbtn" data-action="close" aria-label="ปิดหน้าต่าง">×</button></div>`}
function art(theme){
 if(theme==='tech')return `<svg viewBox="0 0 600 210" aria-hidden="true"><rect width="600" height="210" fill="#242332"/><g stroke="#635177" fill="none" stroke-width="1" opacity=".6"><path d="M0 40h220v45h40M0 100h180v50h80M600 50H390v32h-30M600 155H425v-20h-55M290 0v45M340 210v-38"/><circle cx="180" cy="100" r="4" fill="#c6b7ea"/><circle cx="425" cy="155" r="4" fill="#c6b7ea"/></g><rect x="249" y="30" width="104" height="152" rx="20" fill="#161922" stroke="#9e8cbc" stroke-width="2"/><rect x="260" y="46" width="82" height="119" rx="12" fill="#343040"/><rect x="281" y="38" width="39" height="3" rx="2" fill="#867495"/><circle cx="301" cy="100" r="28" fill="#d0fa77"/><path d="M287 104v-7m9 15V88m10 18V94m9 8v-8" stroke="#263419" stroke-width="4" stroke-linecap="round"/><rect x="115" y="44" width="68" height="33" rx="10" fill="#343046" stroke="#625375"/><text x="149" y="65" fill="#c6b7ea" font-family="monospace" text-anchor="middle" font-size="11">LOCAL</text><rect x="420" y="90" width="75" height="33" rx="10" fill="#343046" stroke="#625375"/><text x="457" y="111" fill="#d0fa77" font-family="monospace" text-anchor="middle" font-size="11">VOICES</text></svg>`;
 if(theme==='garden')return `<svg viewBox="0 0 600 210" aria-hidden="true"><rect width="600" height="210" fill="#24301d"/><path d="m60 170 240-105 240 105-240 45z" fill="#36432b"/><g fill="none" stroke="#829263"><ellipse cx="300" cy="142" rx="180" ry="49"/><ellipse cx="300" cy="142" rx="128" ry="34"/></g><path d="M268 117h68l-11 67h-46z" fill="#8e7750"/><path d="M302 127V56" stroke="#a9c780" stroke-width="5"/><path d="M302 98Q238 93 251 44q51 1 51 54" fill="#b7d879"/><path d="M304 80q-3-62 54-55 0 53-54 55" fill="#d0fa77"/><path d="M124 124h44l-8 44h-28z" fill="#6d6e4d"/><path d="M146 125q-47-9-29-46 38 7 29 46m0-6q-4-47 34-42 0 37-34 42" fill="#779e6c"/><path d="M442 128h40l-8 39h-24z" fill="#6d6e4d"/><path d="M463 128q-46-6-27-39 32 0 27 39m0-7q-8-49 32-38 2 31-32 38" fill="#9aba77"/></svg>`;
 if(theme==='transit')return `<svg viewBox="0 0 600 210" aria-hidden="true"><rect width="600" height="210" fill="#302a1e"/><path d="M-20 155 140 80h240l240 96" fill="none" stroke="#584e36" stroke-width="46"/><path d="M-20 155 140 80h240l240 96" fill="none" stroke="#9e8d5e" stroke-width="2" stroke-dasharray="11 10"/><rect x="205" y="44" width="150" height="78" rx="16" fill="#d1bd82"/><path d="M224 59h103v29H224z" fill="#454b34"/><path d="M252 58v31m36-31v31" stroke="#d1bd82" stroke-width="5"/><circle cx="238" cy="121" r="14" fill="#171f17"/><circle cx="323" cy="121" r="14" fill="#171f17"/><circle cx="238" cy="121" r="6" fill="#a9b297"/><circle cx="323" cy="121" r="6" fill="#a9b297"/><path d="M435 142V62" stroke="#a3966d" stroke-width="5"/><rect x="419" y="38" width="34" height="39" rx="7" fill="#d0fa77"/><text x="436" y="64" text-anchor="middle" fill="#25351a" font-family="monospace" font-size="18">P</text><path d="m125 148 10-7 10 7m-10-7v30" stroke="#d0fa77" fill="none" stroke-width="3"/></svg>`;
 return `<svg viewBox="0 0 600 210" aria-hidden="true"><rect width="600" height="210" fill="#1c3026"/><g stroke="#35503a" fill="none" opacity=".6"><path d="M0 30h600M0 65h600M0 100h600M0 135h600M0 170h600M0 205h600M50 0v210M100 0v210M150 0v210M200 0v210M250 0v210M300 0v210M350 0v210M400 0v210M450 0v210M500 0v210M550 0v210"/></g><path d="M198-20q120 52 77 104t87 144" fill="none" stroke="#386357" stroke-width="61"/><path d="M198-20q120 52 77 104t87 144" fill="none" stroke="#76b9a4" stroke-width="1" stroke-dasharray="5 7"/><path d="m-20 151 640-40" stroke="#596347" stroke-width="22"/><path d="m-20 151 640-40" stroke="#cbd3aa" stroke-width="1.5" stroke-dasharray="10 8"/><g fill="#43563b" stroke="#687b52" stroke-width="1"><path d="m106 45 51-4 2 34-51 4zM378 33l60 3-3 38-60-3zM419 149l66-4 3 33-66 4zM99 177l52-3 2 21-52 3z"/></g><g fill="none" stroke="#a2d46b"><circle cx="257" cy="133" r="25" opacity=".3"/><circle cx="257" cy="133" r="39" opacity=".2"/><circle cx="417" cy="124" r="26" opacity=".25"/></g><circle cx="257" cy="133" r="8" fill="#d0fa77"/><circle cx="417" cy="124" r="7" fill="#edc57f"/><circle cx="166" cy="139" r="6" fill="#8bd5ca"/><rect x="230" y="81" width="56" height="25" rx="7" fill="#d0fa77"/><text x="258" y="98" text-anchor="middle" fill="#263819" font-size="11" font-family="monospace">01</text><rect x="389" y="73" width="56" height="25" rx="7" fill="#e7c383"/><text x="417" y="90" text-anchor="middle" fill="#423d26" font-size="11" font-family="monospace">02</text></svg>`;
}
function eventCard(r){const s=roomStats(r.id,store),voices=roomVoices(r.id,store),states=[...new Set(r.claims.map(c=>getClaimState(c,store).status))];return `<article class="card" data-room="${h(r.id)}"><div class="card-top"><span class="tag ${r.theme==='tech'?'purple':r.theme==='transit'?'amber':'teal'}">● ${h(r.category)}</span><span class="micro">${r.local?'เฉพาะเครื่องนี้':'ข้อมูลจำลอง · '+h(r.updated)}</span></div><div class="card-body"><a class="card-title-link" href="#/room/${h(r.id)}"><h2>${h(r.title)}</h2></a><div class="card-sub"><span>⌁ ${h(r.place)}</span></div><a href="#/room/${h(r.id)}" aria-label="เข้าห้อง ${h(r.title)}"><div class="art art-${h(r.theme)}">${art(r.theme)}<span class="art-label">ภาพประกอบเชิงแนวคิด · ไม่ใช่พื้นที่จริง</span><span class="art-top">ECHO VOICE ROOM ↗</span></div></a><div class="card-state">${(states.length?states:['UNKNOWN']).map(st=>`<span class="state-pill ${cls(st)}">${stateMark(st)} ${stateLabel(st)}</span>`).join('')}${s.pending?`<span class="mini-badge">${s.pending} เสียงใหม่รอตรวจ</span>`:''}</div><div class="voices-peek"><span class="avatar-stack">${voices.slice(0,3).map((v,i)=>avatar(v.name,i)).join('')}</span><span class="peek-text"><strong>${s.voices} เสียง</strong> · ${s.people} ผู้ร่วมโพสต์</span><a class="room-cta" href="#/room/${h(r.id)}">เข้าห้องเสียง ↗</a></div><p class="peek-quote">${h(r.quote)}</p></div><div class="card-footer"><button data-action="follow" data-id="${h(r.id)}" class="${store.following.includes(r.id)?'active':''}" aria-pressed="${store.following.includes(r.id)}">${icon('follow')}${store.following.includes(r.id)?'ติดตามแล้ว':'ติดตามเรื่องนี้'}</button><button data-action="compose" data-id="${h(r.id)}">${icon('plus')}เพิ่มเสียง</button><button data-action="share" data-id="${h(r.id)}">${icon('share')}แชร์ห้อง</button></div></article>`}
function renderHome(following=false){const rooms=filterRooms(store,{query,topic,following,latest:feedTab==='latest'});main.innerHTML=`<div class="page-heading"><div><span class="eyebrow">MANY VOICES. ONE BIG PICTURE.</span><h1>${following?'ห้องที่คุณ<span class="heading-accent">ติดตาม</span>':'ทุกเรื่องราว<span class="heading-accent">มีหลายเสียง</span>'}</h1><p>${following?'กลับมาติดตามเรื่องที่สนใจ · บันทึกเฉพาะเครื่องนี้':'ข่าวเดียวกัน ไม่ต้องไถหาจากร้อยโพสต์'}</p></div><span class="index-tag">${rooms.length} ห้อง${query?'ที่พบ':'ทดลอง'}</span></div><div class="tabs" aria-label="เรียงฟีด"><button data-action="feed-tab" data-value="discover" class="${feedTab==='discover'?'active':''}" aria-pressed="${feedTab==='discover'}">สำรวจเหตุการณ์</button><button data-action="feed-tab" data-value="latest" class="${feedTab==='latest'?'active':''}" aria-pressed="${feedTab==='latest'}">ตามเวลาในเดโม</button></div><div class="filters" aria-label="หมวดเหตุการณ์">${TOPICS.map(t=>`<button class="chip ${topic===t?'active':''}" data-action="topic" data-value="${h(t)}" aria-pressed="${topic===t}">${h(t)}</button>`).join('')}</div>${rooms.map(eventCard).join('')||`<div class="empty"><span class="empty-icon">◎</span><h2>${following?'ยังไม่มีห้องที่ตรงกับตัวกรอง':'ไม่พบเหตุการณ์ที่ค้นหา'}</h2><p>${following?'กด “ติดตามเรื่องนี้” บนการ์ดข่าวเพื่อเก็บห้องไว้':'ลองค้นคำสั้น ๆ หรือเลือกหมวดทั้งหมด'}</p><button class="secondary" data-action="clear-filters">ดูทุกห้อง</button></div>`}<div class="page-end">คุณเห็นครบ ${rooms.length} ห้องในชุดทดลองแล้ว · ไม่มีฟีดข่าวสดหรือการดึงข้อมูลภายนอก</div>`}
function voiceCard(v,{original=false}={}){const label=KINDS[v.kind]||'เสียง';return `<article class="voice-card" id="voice-${h(v.id)}"><div class="voice-head">${avatar(v.name,Object.keys(KINDS).indexOf(v.kind))}<div class="voice-byline"><strong>${h(v.name)}${v.local?' · คุณ':''}</strong><small>${v.local?'โพสต์ทดลองในเครื่อง':'บุคคลสมมติ'} · ${h(v.time)}</small></div><span class="type-tag">${h(label)}</span></div><p class="voice-text">${h(v.text)}</p>${v.evidence?`<button class="evidence-link" data-action="evidence" data-id="${h(v.id)}">◇ ${h(v.evidence.label)} ↗</button>`:''}${v.link?`<a class="evidence-link" href="${h(safeURL(v.link))}" target="_blank" rel="noopener noreferrer">↗ ลิงก์ที่คุณแนบ · ยังไม่ได้ตรวจสอบ</a>`:''}<div class="voice-footer"><span>${v.local?`<span class="local-label">${v.shared?'รอตรวจ · ไม่เปลี่ยนสถานะหลักฐาน':'เฉพาะฟีดฉัน · ไม่รวมในห้อง'}</span>`:'เสียงต้นฉบับในชุดจำลอง'} </span>${!original?`<button class="text-button" data-action="original" data-id="${h(v.id)}">ดูโพสต์ต้นฉบับ ↗</button>`:''}</div>${v.local&&original?`<div class="delete-row"><button class="danger" data-action="delete-ask" data-id="${h(v.id)}">ถอนโพสต์ทดลองนี้</button></div>`:''}</article>`}
function reality(r){const pending=roomStats(r.id,store).pending;return `<section class="reality" aria-label="สถานการณ์จากหลักฐานจำลอง"><div class="section-title"><h2>◈ สถานการณ์จากหลักฐาน</h2><span class="eyebrow">REALITY LAYER</span></div>${r.claims.length?r.claims.map(c=>{const s=getClaimState(c,store);return `<div class="claim ${cls(s.status)}"><span class="claim-dot">${stateMark(s.status)}</span><div class="claim-content"><strong>${h(c.text)}</strong><small>${stateLabel(s.status)} · ${h(c.scope)}</small></div><button data-action="claim" data-id="${h(c.id)}">ดูที่มา ↗</button></div>`}).join(''):`<div class="claim unknown"><span class="claim-dot">?</span><div class="claim-content"><strong>ยังไม่มีข้อกล่าวอ้างที่ผ่านการประเมิน</strong><small>เสียงที่เพิ่มเข้ามาไม่ถูกนำไปยืนยันข่าวอัตโนมัติ</small></div></div>`}${pending?`<div class="pending-strip">◷ มี ${pending} เสียงใหม่รอตรวจ · สถานะข้างต้นยังไม่รวมข้อมูลใหม่เหล่านี้</div>`:''}<div class="snapshot-note">ผลประเมินที่กำหนดไว้ในชุดจำลอง · ความสดปัจจุบันไม่ทราบ · ไม่ใช้ตัดสินใจเรื่องความปลอดภัย</div></section>`}
function orbit(r){const g=groups(r.id,store);return `<div class="constellation" aria-label="วงเสียง แบ่งตามประเภท ไม่ใช่สัดส่วนความคิดเห็นประชากร"><svg viewBox="0 0 600 240" preserveAspectRatio="none" aria-hidden="true"><ellipse cx="300" cy="120" rx="178" ry="74" fill="none" stroke="#5c7344" stroke-dasharray="3 6"/><path d="m110 62 190 58 190-58M120 185l180-65 180 65" stroke="#6e8652" stroke-width="1" fill="none"/></svg><div class="const-center"><b>${roomStats(r.id,store).voices}</b>echo voices</div>${g.map(x=>`<button class="cluster-node k-${x.kind}" data-action="kind" data-value="${x.kind}" aria-label="ดู${h(x.label)} ${x.voices.length} เสียง"><b>${x.voices.length}</b><span>${h(x.label)}<small>แตะเพื่อเจาะลึก ↗</small></span></button>`).join('')}</div><p class="group-desc">วงเสียงจัดตามประเภทในข้อมูลตัวอย่างและที่ผู้ใช้เลือก ไม่ใช่ผลวิเคราะห์ AI หรือผลสำรวจประชาชน</p>${g.filter(x=>x.voices.length).map(x=>`<div class="divider-title"><h3>${h(x.label)}</h3><button class="text-button" data-action="kind" data-value="${x.kind}">ดู ${x.voices.length} เสียง ↗</button></div>${voiceCard(x.voices[x.voices.length-1])}`).join('')}`}
function roomBody(r){if(roomTab==='orbit')return orbit(r);if(roomTab==='evidence'){const vs=roomVoices(r.id,store).filter(v=>v.evidence);return `<div class="notice">${vs.length} รายการ · ${roomStats(r.id,store).families} กลุ่มต้นทางตัวอย่าง<br><strong>หลายกลุ่มไม่ได้พิสูจน์ว่าเป็นอิสระจากกัน</strong> และลิงก์ที่เพิ่มใหม่ยังไม่ใช่หลักฐานที่รับรองแล้ว</div><div class="divider-title"><h3>หลักฐานในชุดจำลอง</h3></div>${vs.map(voiceCard).join('')||'<div class="empty"><h2>ยังไม่มีหลักฐานที่ประเมินแล้ว</h2><p>โพสต์หรือลิงก์ใหม่ยังอยู่ในคิวทดลอง ไม่เปลี่ยนข้อสรุปอัตโนมัติ</p></div>'}`}
 if(roomTab==='timeline'){const vs=roomVoices(r.id,store);return `<p class="group-desc">เส้นเวลาจำลองแยกจากโพสต์ในเครื่อง ไม่ใช่ลำดับข่าวสด</p>${vs.filter(v=>!v.local).sort((a,b)=>a.time.localeCompare(b.time)).map(v=>`<div class="timeline-item"><time>${h(v.time)} · จำลอง</time><h3>${h(v.name)} · ${h(KINDS[v.kind])}</h3><p>${h(v.text)}</p><button class="text-button" data-action="original" data-id="${h(v.id)}">ดูโพสต์ต้นฉบับ ↗</button></div>`).join('')}${vs.some(v=>v.local)?'<h3>โพสต์ที่เพิ่มในเครื่องนี้</h3>':''}${vs.filter(v=>v.local).map(voiceCard).join('')}`}
 const vs=roomVoices(r.id,store).filter(v=>!kindFilter||v.kind===kindFilter);return `<div class="filters"><button class="chip ${!kindFilter?'active':''}" data-action="kind" data-value="">ทั้งหมด</button>${Object.entries(KINDS).map(([k,l])=>`<button class="chip ${kindFilter===k?'active':''}" data-action="kind" data-value="${k}">${h(l)}</button>`).join('')}</div>${vs.map(voiceCard).join('')||'<div class="empty"><h2>ยังไม่มีเสียงในกลุ่มนี้</h2><p>ลองเพิ่มเสียงทดลองของคุณ หรือดูประเภทอื่น</p></div>'}`}
function renderRoom(id){const r=allRooms(store).find(x=>x.id===id);if(!r){main.innerHTML='<div class="empty"><h2>ไม่พบห้องนี้</h2><p>ห้องที่สร้างในเดโมอาจมีเฉพาะในเครื่องของผู้สร้าง</p><a class="primary" href="#/">กลับศูนย์รวมข่าว</a></div>';return}if(lastRoom!==id){roomTab='orbit';kindFilter='';lastRoom=id}const s=roomStats(id,store);main.innerHTML=`<a class="back-link" href="#/">← กลับศูนย์รวมข่าว</a><section class="room-header"><span class="eyebrow">ECHO VOICE ROOM / ${r.local?'LOCAL':'DEMO'}</span><h1>${h(r.title)}</h1><div class="room-meta"><span>${h(r.place)}</span><span>·</span><span>${s.voices} เสียง</span><span>·</span><span>${s.people} ผู้ร่วมโพสต์${r.local?'ในเครื่อง':'สมมติ'}</span></div><div class="room-actions"><button class="primary" data-action="compose" data-id="${h(id)}">＋ เพิ่มเสียงในห้อง</button><button class="secondary ${store.following.includes(id)?'active':''}" data-action="follow" data-id="${h(id)}" aria-pressed="${store.following.includes(id)}">${store.following.includes(id)?'✓ ติดตามแล้ว':'◎ ติดตาม'}</button><button class="secondary" data-action="share" data-id="${h(id)}" aria-label="แชร์ห้องนี้">↗</button></div></section>${reality(r)}<div class="society-title"><h2>ผู้คนกำลังพูดอะไร</h2><span class="voice-count">${s.voices} VOICES</span></div><p class="sublabel">เสียงต้นฉบับที่เกี่ยวข้องกับเรื่องนี้ · ไม่ใช่ความคิดเห็นของคนทั้งสังคม</p><div class="tabs room-tabs">${[['orbit','วงเสียง'],['all','โพสต์ทั้งหมด'],['evidence','หลักฐาน'],['timeline','เส้นเวลา']].map(([key,label])=>`<button data-action="room-tab" data-value="${key}" class="${roomTab===key?'active':''}" aria-pressed="${roomTab===key}">${label}</button>`).join('')}</div><section id="room-body">${roomBody(r)}</section><div class="page-end">หนึ่งเหตุการณ์ หลายเสียง ทุกเสียงมีที่มา · Research Preview</div>`}
function renderMine(){main.innerHTML=`<div class="page-heading"><div><span class="eyebrow">YOUR ORIGINAL VOICES</span><h1>เสียง<span class="heading-accent">ของฉัน</span></h1><p>โพสต์ต้นฉบับยังอยู่ที่นี่ แม้จะเชื่อมเข้า Echo Voice Room</p></div></div><div class="local-profile">${avatar('คุณ',3)}<div><h2>พื้นที่ทดลองของคุณ</h2><p>ไม่มีบัญชี Login · ${store.voices.length} โพสต์ในเบราว์เซอร์นี้</p></div></div><div class="notice">โพสต์ไม่ถูกส่งขึ้นเซิร์ฟเวอร์ คนอื่นมองไม่เห็น แม้จะเลือกเชื่อมเข้าห้องก็ตาม อย่าใส่ข้อมูลส่วนตัวหรือใช้แจ้งเหตุฉุกเฉิน</div><div class="divider-title"><h3>โพสต์ของคุณ</h3><button class="text-button" data-action="compose">＋ เขียนเสียงใหม่</button></div>${[...store.voices].reverse().map(v=>voiceCard(v,{original:true})).join('')||'<div class="empty"><span class="empty-icon">▤</span><h2>เสียงแรกเริ่มจากคุณ</h2><p>ลองเขียนโพสต์ แล้วดูว่าปรากฏในห้องข่าวอย่างไร</p><button class="primary" data-action="compose">＋ เพิ่มเสียงของคุณ</button></div>'}`}
function aboutContent(){return `<div class="about-hero"><span class="eyebrow">THIS IS ECHO NEWS CENTER</span><h1>หนึ่งเหตุการณ์<br>หลายเสียงที่ควรได้ยิน.</h1><p>ไม่ใช่บทความที่เขียนแทนทุกคน แต่เป็นพื้นที่กลางที่พาคุณไปถึงเสียงต้นฉบับ หลักฐาน และสิ่งที่ยังไม่ทราบ</p></div>${[['01','เสียงของคุณยังเป็นของคุณ','คนโพสต์ในฟีดของตัวเอง ห้องข่าวเชื่อมเสียงที่เกี่ยวข้องมาแสดง ไม่หลอมให้เหลือความเห็นเดียว'],['02','สิ่งที่รู้ กับสิ่งที่คนพูด','Reality Layer แสดงสถานะจากหลักฐานที่มี Society Layer แสดงรายงาน ความคิดเห็น คำถาม และข้อทักท้วง'],['03','ยอดแชร์ไม่ใช่ความจริง','การแชร์ต้นทางเดียวไม่เพิ่มหลักฐานอิสระ การเพิ่มเสียงใหม่ในเดโมไม่เปลี่ยนสถานะข้อกล่าวอ้าง'],['04','เจาะลงไปดูที่มาได้','กดจากข้อกล่าวอ้างไปยังบันทึกต้นทางและโพสต์สมมติที่ใช้สร้างผลประเมิน'],['05','ยังเป็นงานทดลอง ไม่ใช่บริการข่าวจริง','ทุกเหตุการณ์และบุคคลเริ่มต้นเป็นข้อมูลสมมติ โพสต์ใหม่และห้องที่ติดตามเก็บเฉพาะเบราว์เซอร์นี้ ไม่มี AI ตรวจข่าว ไม่มีการดึงโซเชียลภายนอก และยังไม่เชื่อม API หรือฐานข้อมูล P2.1']].map(([n,t,d])=>`<div class="principle"><span>${n}</span><div><h3>${t}</h3><p>${d}</p></div></div>`).join('')}<div class="notice"><strong>ขอบเขตความปลอดภัย</strong><br>ห้ามใช้ข้อมูลเดโมเพื่อตัดสินใจเดินทางหรือแจ้งเหตุฉุกเฉิน การซ่อนโพสต์ในเครื่องเป็นเพียงพฤติกรรม UI ไม่ใช่ระบบสิทธิ์ผู้ใช้บนเซิร์ฟเวอร์</div><div class="divider-title"><h3>ข้อมูลทดลองในเครื่อง</h3><button class="danger" data-action="reset-ask">ล้างข้อมูลทดลองของฉัน</button></div><p class="prose">การล้างจะลบโพสต์ ห้องที่สร้าง และรายการติดตามเฉพาะในเบราว์เซอร์นี้ ไม่กระทบข้อมูลของคนอื่น และไม่มีข้อมูลถูกส่งให้เรา</p><p class="page-end">Echo News Center · v${VERSION} · Static research preview</p>`}
function renderRight(){const rs=allRooms(store).slice(0,4);$('#right-rail').innerHTML=`<section class="side-block"><div class="side-head"><h3>สำรวจห้องเสียง</h3><span>DEMO ROOMS</span></div>${rs.map((r,i)=>`<a class="room-mini" href="#/room/${h(r.id)}"><span class="num">0${i+1}</span><span><strong>${h(r.short)}</strong><small>${h(r.category)} · ${roomStats(r.id,store).voices} เสียง</small></span></a>`).join('')}</section><section class="side-block"><div class="side-head"><h3>ทำไมต้อง “Echo”?</h3><span>OUR IDEA</span></div><div class="echo-orbit" aria-hidden="true"><div class="orbit-ring outer"></div><div class="orbit-ring"></div><span class="orbit-core">e</span><span class="orbit-dot d1">ม</span><span class="orbit-dot d2">น</span><span class="orbit-dot d3">ฝ</span><span class="orbit-dot d4">ต</span></div><p class="orbit-explainer">ไม่ใช่เสียงสะท้อนที่พูดเหมือนกัน<br>แต่คือ <strong>หลายเสียงรอบเหตุการณ์เดียว</strong> ที่ยังเห็นความต่างและที่มา</p><a class="small-link" href="#/about">รู้จักแนวคิดของเรา ↗</a></section><div class="notice">◇ จำนวนเสียง ≠ ความจริง<br>◇ หลายกลุ่มต้นทาง ≠ ยืนยันความเป็นอิสระ<br>◇ สรุปจำลอง ≠ ข่าวที่ตรวจแล้ว</div><p class="right-foot">พื้นที่ทดลองออกแบบ Social News<br>ข้อมูลสมมติทั้งหมด · ไม่ใช่ข่าวสด</p>`}
function render(){const parts=(location.hash||'#/').slice(1).split('/').filter(Boolean),page=parts[0]||'home';document.querySelectorAll('[data-nav]').forEach(a=>a.classList.toggle('active',a.dataset.nav===(page==='room'?'home':page)));if(page==='room')renderRoom(parts[1]);else if(page==='mine')renderMine();else if(page==='about')main.innerHTML=aboutContent();else if(page==='following')renderHome(true);else if(page==='home'||!parts.length)renderHome();else main.innerHTML='<div class="empty"><h2>ไม่พบหน้านี้</h2><a class="primary" href="#/">กลับหน้าหลัก</a></div>';renderRight();document.title=page==='room'?(allRooms(store).find(r=>r.id===parts[1])?.short||'ห้องเสียง')+' · Echo Demo':'Echo News Center · ทุกเสียงมีที่มา'}
function composer(id){const current=allRooms(store).some(r=>r.id===id)?id:allRooms(store)[0].id;openModal(`${modalHead('เพิ่มเสียงของคุณ')}<div class="notice"><strong>บันทึกในเครื่องนี้เท่านั้น</strong> ไม่โพสต์สาธารณะ ไม่ส่งไปยังผู้ใช้อื่น และไม่ใช่ช่องทางแจ้งเหตุฉุกเฉิน</div><form id="compose-form"><div class="form-split"><div class="form-row"><label for="voice-name">ชื่อที่แสดงในเดโม</label><input id="voice-name" name="name" value="คุณ" maxlength="40" required autocomplete="off"></div><div class="form-row"><label for="voice-kind">เสียงของคุณเป็นแบบไหน</label><select id="voice-kind" name="kind">${Object.entries(KINDS).map(([k,l])=>`<option value="${k}">${h(l)}</option>`).join('')}</select></div></div><div class="form-row"><label for="voice-room">เชื่อมกับเหตุการณ์</label><select id="voice-room" name="roomId">${allRooms(store).map(r=>`<option value="${h(r.id)}" ${r.id===current?'selected':''}>${h(r.short)}</option>`).join('')}<option value="__new__">＋ สร้างห้องทดลองใหม่</option></select><small>รุ่นนี้คุณเลือกห้องเอง ยังไม่มี AI จับเหตุการณ์ให้อัตโนมัติ</small></div><div class="form-row hidden" id="new-room-row"><label for="new-title">ชื่อเหตุการณ์ทดลองใหม่</label><input id="new-title" name="roomTitle" maxlength="140" placeholder="ตั้งชื่อเรื่องที่อยากทดลอง" autocomplete="off"></div><div class="form-row"><label for="voice-text">คุณพบอะไร หรืออยากเพิ่มเติมอะไร?</label><textarea id="voice-text" name="text" maxlength="2000" required placeholder="เล่าสิ่งที่รู้ ระบุเวลาและพื้นที่เมื่อมีข้อมูล แยกสิ่งที่เห็นจากสิ่งที่คิด…"></textarea><small>ไม่เกิน 2,000 ตัวอักษร · อย่าใส่ข้อมูลส่วนบุคคล</small></div><div class="form-row"><label for="voice-link">ลิงก์ประกอบ (ไม่บังคับ)</label><input id="voice-link" name="link" type="url" placeholder="https://…"><small>ระบบไม่ดึงข้อมูลลิงก์ และยังไม่ประเมินว่าเป็นหลักฐาน</small></div><label class="form-check"><input type="checkbox" name="shared" checked><span>เชื่อมเสียงนี้เข้าห้องในเดโมด้วย<br>ถ้าไม่เลือก จะเห็นเฉพาะใน “เสียงของฉัน”</span></label><p class="form-error" id="form-error" role="alert"></p><div class="form-actions"><small>โพสต์ใหม่อยู่ในสถานะรอตรวจ<br>ไม่เปลี่ยนผลประเมินจำลอง</small><button class="primary" type="submit">เพิ่มเสียงในเครื่อง ↗</button></div></form>`)}
function original(id){const v=allVoices(store).find(v=>v.id===id);if(!v)return;const r=allRooms(store).find(r=>r.id===v.roomId);openModal(`${modalHead('เสียงต้นฉบับ')}${voiceCard(v,{original:true})}<div class="trace-row"><span class="step">↗</span><div><h3>ต้นฉบับ → ห้องที่เกี่ยวข้อง</h3><p>${v.local&&!v.shared?'โพสต์นี้ไม่ได้เชื่อมเข้าห้อง':'เชื่อมกับ '+h(r?.title||'ห้องไม่ทราบ')}</p>${!v.local||v.shared?`<button class="text-button" data-action="goto-room" data-id="${h(v.roomId)}">ไปยังห้องข่าว ↗</button>`:''}</div></div><p class="prose">${v.local?'รหัสโพสต์ทดลอง: '+h(v.id):'บุคคลและข้อความนี้เป็นข้อมูลสมมติที่สร้างไว้สำหรับทดสอบ UX ไม่ใช่โพสต์จากโซเชียลจริง'}</p>`)}
function evidence(id){const v=allVoices(store).find(v=>v.id===id);if(!v?.evidence)return;const e=v.evidence,related=allVoices(store).filter(x=>x.evidence?.family===e.family);openModal(`${modalHead('หลักฐานและเส้นทางต้นทาง')}<div class="notice"><strong>หลักฐานจำลอง</strong> เป็นข้อมูลที่เขียนขึ้นสำหรับทดสอบหน้าจอ ไม่ได้ตรวจภาพหรือเหตุการณ์จริง</div><div class="trace-row"><span class="step">01</span><div><h3>${h(e.label)}</h3><p>${h(e.note)}</p></div></div><div class="trace-arrow">↓</div><div class="trace-row"><span class="step">02</span><div><h3>${h(e.origin)}</h3><p>Evidence ID: ${h(e.id)}<br>Family: ${h(e.family)}<br>${related.length} รายการในกลุ่มต้นทางนี้ · ยังไม่ได้ประเมินความเป็นอิสระ</p></div></div><div class="trace-arrow">↓</div><div class="trace-row"><span class="step">03</span><div><h3>กลับไปยังเสียงต้นฉบับ</h3>${related.map(x=>`<p><button class="text-button" data-action="original" data-id="${h(x.id)}">${h(x.name)} · ${h(x.time)} · ${h(x.id)} ↗</button></p>`).join('')}</div></div>`)}
function claimDetail(id){let c,r;for(const room of allRooms(store)){const found=room.claims.find(x=>x.id===id);if(found){c=found;r=room;break}}if(!c)return;const s=getClaimState(c,store);openModal(`${modalHead('ข้อกล่าวอ้างและที่มา')}<span class="state-pill ${cls(s.status)}">${stateMark(s.status)} ${stateLabel(s.status)}</span><h3 class="divider-title">${h(c.text)}</h3><p class="prose">${h(c.scope)}</p><div class="notice">ผลประเมินนี้มาจาก fixture ที่กำหนดไว้ ไม่ใช่ AI ตรวจข่าวจริง ความสดปัจจุบันและความเป็นอิสระของแหล่งข้อมูลยังไม่ทราบ</div>${[['สนับสนุน',s.support],['คัดค้าน',s.counter]].map(([label,ids])=>`<div class="divider-title"><h3>${label} · ${ids.length} รายการอ้างอิง</h3></div>${ids.map(vid=>{const v=allVoices(store).find(x=>x.id===vid);return `<div class="trace-row"><span class="step">◇</span><div><h3>${h(v?.evidence?.label||'บันทึกตัวอย่าง')}</h3><p>${h(v?.name)} · ${h(v?.time)}<br>${h(v?.evidence?.family||'ยังไม่จัดกลุ่มต้นทาง')}</p><button class="text-button" data-action="original" data-id="${h(vid)}">ดูเสียงต้นฉบับ ↗</button></div></div>`}).join('')||'<p class="prose">ยังไม่มีข้อมูลในชุดตัวอย่าง</p>'}`).join('')}<p class="prose">Claim: ${h(c.id)} · Room: ${h(r.id)}<br>การเพิ่มความคิดเห็นหรือโพสต์ใหม่ไม่เปลี่ยนป้ายนี้อัตโนมัติ</p>`)}
async function share(id){const r=allRooms(store).find(r=>r.id===id);if(!r)return;if(r.local){toast('ห้องนี้มีเฉพาะเครื่องคุณ จึงยังแชร์ให้คนอื่นเปิดไม่ได้');return}const u=new URL(location.href);u.hash='/room/'+id;try{await navigator.clipboard.writeText(u.href);toast('คัดลอกลิงก์ห้องตัวอย่างแล้ว · ไม่มีโพสต์ในเครื่องติดไปด้วย')}catch{openModal(`${modalHead('ลิงก์ห้องตัวอย่าง')}<p class="prose">แชร์เฉพาะห้องจำลอง ไม่รวมโพสต์ที่เก็บในเครื่อง</p><input class="share-url" readonly aria-label="ลิงก์สำหรับแชร์" value="${h(u.href)}">`);$('.share-url').select()}}
document.addEventListener('click',async e=>{const b=e.target.closest('[data-action]');if(!b)return;const a=b.dataset.action,id=b.dataset.id,value=b.dataset.value;if(b.tagName==='BUTTON'&&b.type!=='submit')e.preventDefault();
 if(a==='close'){modal.close();return}if(a==='about'){location.hash='/about';return}if(a==='profile'){location.hash='/mine';return}if(a==='search'){$('.topbar').classList.toggle('search-open');$('#search').focus();return}if(a==='compose'){composer(id||((location.hash||'').startsWith('#/room/')?location.hash.split('/')[2]:''));return}if(a==='original'){original(id);return}if(a==='evidence'){evidence(id);return}if(a==='claim'){claimDetail(id);return}if(a==='goto-room'){modal.close();location.hash='/room/'+id;return}if(a==='share'){await share(id);return}
 if(a==='follow'){if(save(toggleFollow(store,id))){render();toast(store.following.includes(id)?'เก็บห้องนี้ในรายการติดตามแล้ว · เฉพาะเครื่องนี้':'เลิกติดตามห้องนี้แล้ว')}return}
 if(a==='topic'){topic=value;feedTab='discover';if(!['#/','#/following',''].includes(location.hash))location.hash='/';else render();return}if(a==='clear-filters'){topic='ทั้งหมด';query='';$('#search').value='';feedTab='discover';if(location.hash!=='#/')location.hash='/';else render();return}if(a==='feed-tab'){feedTab=value;render();return}if(a==='room-tab'){roomTab=value;kindFilter='';render();return}if(a==='kind'){roomTab='all';kindFilter=value;render();$('#room-body')?.scrollIntoView({block:'start',behavior:'smooth'});return}
 if(a==='delete-ask'){openModal(`${modalHead('ถอนโพสต์ทดลอง?')}<p class="prose">โพสต์นี้จะหายจากฟีดของคุณและห้องในเครื่องนี้ ไม่กระทบข้อมูลตัวอย่างหรือข้อมูลของคนอื่น</p><div class="form-actions"><button class="secondary" data-action="close">ยกเลิก</button><button class="danger" data-action="delete-confirm" data-id="${h(id)}">ถอนโพสต์</button></div>`);return}if(a==='delete-confirm'){if(save(removeVoice(store,id))){modal.close();render();toast('ถอนโพสต์จากฟีดและห้องในเครื่องแล้ว')}return}
 if(a==='reset-ask'){openModal(`${modalHead('ล้างข้อมูลทดลองในเครื่อง?')}<p class="prose">จะลบโพสต์ที่คุณเขียน ห้องที่คุณสร้าง และรายการติดตามในเบราว์เซอร์นี้ การกระทำนี้ย้อนคืนไม่ได้</p><div class="form-actions"><button class="secondary" data-action="close">ยกเลิก</button><button class="danger" data-action="reset-confirm">ล้างข้อมูลของฉัน</button></div>`);return}if(a==='reset-confirm'){if(save(emptyStore())){modal.close();render();toast('ล้างเฉพาะข้อมูลเดโมในเครื่องนี้แล้ว')}return}
});
document.addEventListener('change',e=>{if(e.target.id==='voice-room'){const isNew=e.target.value==='__new__';$('#new-room-row').classList.toggle('hidden',!isNew);$('#new-title').required=isNew}});
document.addEventListener('submit',e=>{if(e.target.id!=='compose-form')return;e.preventDefault();const f=new FormData(e.target);try{let next={...store,rooms:[...store.rooms],voices:[...store.voices]},roomId=String(f.get('roomId'));if(roomId==='__new__'){const room=makeRoom(f.get('roomTitle'),'พื้นที่และชุมชน',crypto.randomUUID(),next);next.rooms.push(room);roomId=room.id}const v=makeVoice({roomId,name:f.get('name'),kind:f.get('kind'),text:f.get('text'),link:f.get('link'),shared:f.get('shared')==='on'},next,crypto.randomUUID());next.voices.push(v);if(!save(next)){$('#form-error').textContent='บันทึกในเครื่องไม่สำเร็จ ข้อความยังอยู่ในฟอร์ม';return}modal.close();roomTab='all';kindFilter='';lastRoom=roomId;if(v.shared){if(location.hash==='#/room/'+roomId)render();else location.hash='/room/'+roomId}else{if(location.hash==='#/mine')render();else location.hash='/mine'}toast(v.shared?'เพิ่มเสียงในเครื่องแล้ว · รอตรวจและไม่เปลี่ยนข้อสรุป':'บันทึกในฟีดของฉันแล้ว · ไม่แสดงในห้อง')}catch(err){$('#form-error').textContent=err.message||'บันทึกไม่สำเร็จ'}});
modal.addEventListener('click',e=>{if(e.target===modal){const rect=modal.getBoundingClientRect();if(e.clientX<rect.left||e.clientX>rect.right||e.clientY<rect.top||e.clientY>rect.bottom)modal.close()}});
modal.addEventListener('close',()=>{if(lastFocus?.isConnected)lastFocus.focus()});
$('#search').addEventListener('input',e=>{query=e.target.value;if(location.hash!=='#/'&&location.hash!=='#/following'&&location.hash!=='')location.hash='/';else render()});
window.addEventListener('hashchange',()=>{render();window.scrollTo(0,0)});
window.addEventListener('storage',e=>{if(e.key===STORAGE_KEY){try{store=e.newValue?normalizeStore(JSON.parse(e.newValue)):emptyStore();render()}catch{toast('ข้อมูลในอีกแท็บอ่านไม่ได้ กรุณาโหลดหน้าใหม่')}}});
$('#topic-nav').innerHTML=TOPICS.slice(1).map(t=>`<button data-action="topic" data-value="${h(t)}"><span class="topic-dot"></span>${h(t)}</button>`).join('');
render();if(storageWarning)toast('อ่านข้อมูลเดิมไม่ได้ กำลังใช้ชุดตัวอย่างโดยไม่ส่งข้อมูลออก');
